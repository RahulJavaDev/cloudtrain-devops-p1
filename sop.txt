echo "# cloudtrain-devops-p1" >> README.md
1.Initialization of git repo in your folder to maintain and track code on local to repo
git init


2.To change name from master to main
git branch -M main

3.
git remote add origin https://github.com/RahulJavaDev/cloudtrain-devops-p1.git

4.git add test-file-1.txt
git add .

5. commiting the changes to local repo
git commit -m "i have add the files for starting steps"

6.pushing the changes from local to remote repo
git push -u origin main

test conflicts

git config --global user.name "Your Name"

git config --global user.email "your_email@example.com"

7.
Viewing branch
git branch

8. Creating new branch
git branch feature/updating-codes

9. Switching branch
git checkout feaure/updating-codes

11. Creating and switching to new branch
git checkout -b feature/updating-codes1

12. Doing git pull 
13. Stashing the changes
git stash

#14 Popping stashed changes

git stash pop

 

#15 merging of branches

git merge feature/updating-codes

name: CI

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:

      - name: Checkout code

        uses: actions/checkout@v4

      - name: Viewing files

        run: ls -la

      - name: viewing files

        run: cat test-file.txt

#16 git status will show the changes

git status

 

#17 git log will show the history of commits

git log

#16 git status will show the changes

git status

 

#17 git log will show the history of commits

git log



#18 deleting a brnach from local and pushing it to remote

git branch -d feature/to-be-deleted

git push origin --delete feature/to-be-deleted